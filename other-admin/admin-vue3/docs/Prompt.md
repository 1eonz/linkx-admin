你是一个精通 Vue 3、Element Plus 和前端设计规范落地的高级工程师。

请严格参考根目录下的 `docs-local\admin-vue3\DESIGN.md` 规范文件，对我接下来提供给你的组件或页面代码进行重构与优化。

【重构指令要求】：
1. 视觉变量对齐：
   - 将代码中硬编码的颜色、间距统一替换为规范中的设计变量或标准的 Tailwind / CSS 变量。
   - 保证容器采用标准的卡片化结构（如 `.modern-card`）与带有图标及虚线的区块标题（`.section-title`）。

2. 警号与特殊文本规范：
   - 所有警号、设备编号、ID 等关键数字类型文本，必须使用等宽字体并赋予专属高亮样式（如 `.police-id`）。

3. 表格与操作按钮规范：
   - 检查并统一所有表格的行内操作按钮：必须使用 link 模式，且强制补全对应的主题图标（查看-View、编辑-Edit、禁用/启用-VideoPause/VideoPlay、删除-Delete）。
   - 检索与重置工具栏必须规范排布，图标与按钮类型保持一致。

4. 保持业务逻辑：
   - 严禁篡改 `<script setup>` 里的响应式数据、事件绑定、函数逻辑与后端接口调用，仅针对 UI 结构、样式类名和组件化进行精简和重构。

请确认你已理解上述设计要求，准备接收代码。